#include <bits/stdc++.h>
using namespace std;
#define ll long long int
#define db double
int main()
{
    int n;
    cin >> n;
    cout << (sqrt(n) == int(sqrt(n)) ? "YES" : "NO") << endl;
    return 0;
}